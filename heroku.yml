setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    POSTGRES_DB: note_db
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres
run:
  web: npm start