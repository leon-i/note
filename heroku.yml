setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    client: Dockerfile
    server: Dockerfile
  config:
    POSTGRES_DB: note_db
    POSTGRES_USER: postgres
    POSTGRES_PASSWORD: postgres
run:
  client: npm start
  server: go run main.go